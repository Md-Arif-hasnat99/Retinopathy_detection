# Comma-separated list of allowed frontend origins for CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Port to run the server on (Render/Railway inject PORT automatically)
PORT=8000
